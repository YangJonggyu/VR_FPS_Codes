using System;
using System.Collections;
using System.Collections.Generic;
using Game.Scripts;
using UnityEngine;

public class LevelManager : MonoBehaviour
{
    public Transform startPosition;

    private void Start()
    {
        //Player.Instance.transform.root.transform.SetPositionAndRotation(startPosition.position, startPosition.rotation);
    }
}
